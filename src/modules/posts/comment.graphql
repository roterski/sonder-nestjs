type Mutation {
  createComment(createCommentInput: CreateCommentInput): Comment
}

type Comment {
  id: Int
  body: String
  parentIds: [Int]
  points: Int
  postId: Int
}

input CreateCommentInput {
  body: String
  parentIds: [Int]
  postId: Int
}
