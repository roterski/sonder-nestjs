type Query {
  getPosts: [Post]
  getPost(id: Int): Post
}

type Mutation {
  createPost(title: String!, body: String): Post
}

type Subscription {
  postCreated: Post
}

type Post {
  id: Int
  title: String
  body: String
  points: Int
  comments: [Comment]
}
