type Query {
  getPosts: [Post]
  post(id: String): Post
}

type Mutation {
  createPost(createPostInput: CreatePostInput): Post
}

type Subscription {
  postCreated: Post
}

type Post {
  id: Int
  title: String
  body: String
  points: String
}

input CreatePostInput {
  title: String
  body: String
}
